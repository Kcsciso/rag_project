# ============================================================
# 新增依赖（均不会触发 PyTorch / vLLM 升级）
# ============================================================
# 安装方式：
#   conda run -n rag_agent pip install -r requirements_new.txt
# ============================================================

pypdf>=6.0          # PDF 文本提取（纯 Python，零编译依赖）
pyngrok>=8.0        # ngrok 内网穿透（已预装）
